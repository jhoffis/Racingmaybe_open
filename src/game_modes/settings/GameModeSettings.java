package game_modes.settings;

public enum GameModeSettings {
    raceAmount, // most points, best time
    points, // first to points, last to points
    leadout, // lead by time? by points
    time, // in race amount, first under,
    lifes, // run untill first to die from Beat time or dnf? or you lose life if behind in time
}
